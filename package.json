{
  "name": "serverless-aws-kinesis-firehose",
  "version": "0.0.1",
  "description": "Serverless plugin for attaching a lambda function as the processor of a given Kinesis Firehose Stream.",
  "repository": {
    "type": "git",
    "url": "git@github.com:bilby91/serverless-aws-kinesis-firehose.git"
  },
  "keywords": [
    "serverless",
    "kinesis",
    "firehose",
    "lambda"
  ],
  "scripts": {
    "test": "./node_modules/.bin/jest index.test.js",
    "lint": "./node_modules/.bin/prettier --list-different **/*.js",
    "lint-fix": "./node_modules/.bin/prettier --write **/*.js"
  },
  "author": "Martin Fernandez <fmartin91@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.393.0"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "prettier": "^1.16.1"
  }
}
